apiVersion: v1
kind: Service
metadata:
  name: hola-mundo-service
  namespace: dev
spec:
  selector:
    app: hola-mundo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
